{
  "name": "annathomasson.co.uk",
  "version": "1.0.0",
  "description": "",
  "main": "none",
  "scripts": {
    "mkdist": "if [ ! -d dist ]; then mkdir dist; fi",
    "images": "npm run mkdist && cp -av src/img dist",
    "start": "npm run images && gulp",
    "build": "gulp build && npm run images"
  },
  "author": "Damon McMinn",
  "license": "All rights reserved",
  "dependencies": {
    "gulp": "^3.9.0",
    "gulp-connect": "^2.2.0",
    "gulp-jade": "^1.1.0",
    "gulp-livereload": "^3.8.1",
    "gulp-minify-css": "^1.2.1",
    "sanitize.css": "^2.0.0"
  },
  "devDependencies": {
    "gulp-sass": "^2.0.4"
  }
}
